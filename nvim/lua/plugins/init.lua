-- This file's content has been cleared.
-- The `lazy.nvim` setup in `init.lua` with `{ import = "plugins" }`
-- automatically loads all *.lua files in this directory.
-- The previous script here was manually loading them, which is redundant
-- and was causing an error.
return {}